- name: Enable or disable configured cluster services on boot
  service:
    name: "{{ item }}"
    enabled: "{{ ha_cluster_start_on_boot }}"
  loop:
    - corosync
    - corosync-qdevice
    - pacemaker
  when: item != "corosync-qdevice" or ha_cluster_internal_qdevice_in_use

