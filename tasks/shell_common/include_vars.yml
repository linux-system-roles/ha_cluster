# SPDX-License-Identifier: MIT
---

- name: Ensure ansible_facts used by role
  ansible.builtin.setup:
    gather_subset: "{{ __ha_cluster_required_facts_subsets }}"
  when: __ha_cluster_required_facts |
    difference(ansible_facts.keys() | list) | length > 0

- name: Record role begin fingerprint
  sr_fingerprint:
    status: begin
    role_name: ha_cluster
    role_path: "{{ role_path }}"
    ansible_play_hosts_all: "{{ ansible_play_hosts_all }}"
    distribution: "{{ ansible_facts['distribution'] }}"
    distribution_version: "{{ ansible_facts['distribution_version'] }}"
    write_log_file: "{{ __ha_cluster_write_log_file }}"

- name: Determine if system is ostree and set flag
  when: not __ha_cluster_is_ostree is defined
  block:
    - name: Check if system is ostree
      ansible.builtin.stat:
        path: /run/ostree-booted
      register: __ostree_booted_stat

    - name: Set flag to indicate system is ostree
      ansible.builtin.set_fact:
        __ha_cluster_is_ostree: "{{ __ostree_booted_stat.stat.exists }}"

- name: Determine if system is booted with systemd
  when: __ha_cluster_is_booted is not defined
  block:
    - name: Run systemctl
      # noqa command-instead-of-module
      ansible.builtin.command:
        cmd: systemctl is-system-running
      register: __is_system_running
      changed_when: false
      failed_when: false
      check_mode: false

    - name: Require installed systemd
      ansible.builtin.fail:
        msg: "Error: This role requires systemd to be installed."
      when: '"No such file or directory" in __is_system_running.msg | d("")'

    - name: Set flag to indicate that systemd runtime operations are available
      ansible.builtin.set_fact:
        # see https://www.man7.org/linux/man-pages/man1/systemctl.1.html#:~:text=is-system-running%20output
        __ha_cluster_is_booted: "{{ __is_system_running.stdout != 'offline' }}"

- name: Set platform/version specific variables
  ansible.builtin.include_vars:
    file: "{{ __vars_file }}"
  loop:
    - "{{ ansible_facts['os_family'] }}.yml"
    - "{{ ansible_facts['distribution'] }}.yml"
    # Enables loading of shared vars between SLES and SLES_SAP
    - >-
      {{ ansible_facts['distribution'].split("_")[0] ~ '_' ~
      ansible_facts['distribution_major_version'] }}.yml
    - >-
      {{ ansible_facts['distribution'].split("_")[0] ~ '_' ~
      ansible_facts['distribution_version'] }}.yml

    - >-
      {{ ansible_facts['distribution'] ~ '_' ~
      ansible_facts['distribution_major_version'] }}.yml
    - >-
      {{ ansible_facts['distribution'] ~ '_' ~
      ansible_facts['distribution_version'] }}.yml
  loop_control:
    loop_var: var_file_item
  vars:
    __vars_file: "{{ role_path }}/vars/{{ var_file_item }}"
  when: __vars_file is file

- name: Set Linux Pacemaker shell specific variables
  ansible.builtin.include_vars:
    file: "{{ role_path }}/vars/shell_{{ ha_cluster_pacemaker_shell }}.yml"
