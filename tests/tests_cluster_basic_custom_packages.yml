- name: Minimal cluster configuration, install custom packages
  hosts: all
  vars_files: vars/main.yml
  vars:
    ha_cluster_cluster_name: "test-cluster"
    __test_extra_package: "{{
        (ansible_facts['distribution'] == 'Fedora')
        | ternary('glusterfs-resource-agents', 'resource-agents-gcp')
      }}"
    ha_cluster_extra_packages:
      - "{{ __test_extra_package }}"

  tasks:
    - block:
        - name: Set up test environment
          include_tasks: tasks/setup_test.yml

        - name: Ensure extra package is not installed
          package:
            name: "{{ __test_extra_package }}"
            state: absent

        - name: Run HA Cluster role
          include_role:
            name: linux-system-roles.ha_cluster

        - name: Get packages status
          package_facts:

        - name: Check installed packages
          assert:
            that:
              - "'{{ __test_extra_package }}' in ansible_facts.packages"

        - name: Check cluster status
          include_tasks: tasks/assert_cluster_running.yml

      tags: tests::verify
