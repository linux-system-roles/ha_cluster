[project]
name = "ha_cluster"
version = "1"

[tool.setuptools]
py-modules = []

[tool.black]
line-length = 80
extend-exclude = "library/sr_fingerprint.py"

[tool.isort]
profile = "black"
line_length = 80
extend_skip = ["library/sr_fingerprint.py"]
